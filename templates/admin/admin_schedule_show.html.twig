{% extends 'base.html.twig' %}

{% block content %}
    <div class="text-center">
        <h1>Page d'administration du {{ day }}</h1>
    </div>
    <hr>
    <div class="mx-2">
        <a href="{{ path('admin_schedule') }}">Retour à la page d'administration des horaires</a>
        <hr>
        {% if notification %}
            <div class="alert alert-info">{{ notification }}</div>
        {% endif %}
        {{ form(form) }}
    </div>
{% endblock %}