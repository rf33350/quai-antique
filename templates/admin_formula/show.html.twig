{% extends 'base.html.twig' %}

{% block content %}
    <div class="text-center">
        <h1>Administration de la formule "{{ formula.title }}"</h1>
    </div>

    <div class="mx-2">
        <a href="{{ path('app_admin_formula_index') }}">Retour à la liste des formules</a>
        <hr>
        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ formula.id }}</td>
                </tr>
                <tr>
                    <th>Title</th>
                    <td>{{ formula.title }}</td>
                </tr>
                <tr>
                    <th>Menu</th>
                    <td>{{ formula.menu }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ formula.description }}</td>
                </tr>
                <tr>
                    <th>Price</th>
                    <td>{{ formula.price }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_admin_formula_edit', {'id': formula.id}) }}"><button class="btn btn-dark">Modifier</button></a>
        <br>
        <br>
        {{ include('admin_formula/_delete_form.html.twig') }}
    </div>
{% endblock %}
