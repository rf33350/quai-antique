<!DOCTYPE html>
<html lang="fr">
<html>
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Quai Antique | Restaurant</title>
        <link rel="icon" href="{{ asset('assets/img/logo-min.png') }}">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        <link href="{{ asset('assets/css/bootstrap.min.css') }}" rel="stylesheet">
        <link href="{{ asset('assets/css/quai_antique.css') }}" rel="stylesheet">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Hind+Madurai:wght@300&family=Lora&family=Montserrat&family=Open+Sans&family=Raleway:ital,wght@0,400;0,600;1,300&display=swap" rel="stylesheet">
    </head>
    <body>

    <nav class="navbar navbar-expand-md navbar-dark mx-2 mt-2">
        <div class="logo-container">
                <a href="{{ path('home') }}" class="d-inline-flex link-body-emphasis text-decoration-none mr-2">
                    <img src="{{ asset('assets/img/logo.PNG') }}" alt="logo du site" class="logo">
                </a>
        </div>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample04" aria-controls="navbarsExample04" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
            <div class="collapse navbar-collapse text-center" id="navbarsExample04">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link active" href="{{ path('menu') }}">Menus</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link active" href="{{ path('card') }}">Carte</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link active" href="{{ path('schedule') }}">Horaires</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link active" href="{{ path('contact') }}">Contact</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link active " href="{{ path('reservation') }}">Réservation</a>
                    </li>
                </ul>
                <ul class="navbar-nav navbar-item-custom">
                    {% if app.user %}
                        <li class="nav-item active">
                            <a class="nav-link active" href="{{ path('account') }}">Mon compte</a>
                        </li>
                        <li class="nav-item active">
                            <a class="nav-link active" href="{{ path('logout') }}">Déconnexion</a>
                        </li>
                        {% if app.user and is_granted('ROLE_ADMIN')%}
                            <li class="nav-item active">
                                <a class="nav-link active" href="{{ path('admin') }}">Administrer le site</a>
                            </li>
                        {% endif %}
                    {% else %}
                        <li class="nav-item active">
                            <a class="nav-link active" href="{{ path('login') }}">Connexion</a>
                        </li>
                        <li class="nav-item active">
                            <a class="nav-link active" href="{{ path('register') }}">Inscription</a>
                        </li>
                    {% endif %}
                </ul>

        </div>
    </nav>

        {% block content %}{% endblock %}


    <footer class="footer text-center mx-2 my-2">
        <a href="{{ path('home') }}" class="d-inline-flex link-body-emphasis text-decoration-none mt-4">
            <img src="{{ asset('assets/img/logo.PNG') }}" alt="logo du site" class="logo">
        </a>
        <!-- Copyright -->
        <div class="text-center p-3">
            <p class="footer-text">© 2023 Copyright: <a class="footer-text" href="{{ path('home') }}">Le Quai Antique Restaurant</a></p>
        </div>
        <!-- Copyright -->
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="{{ asset('assets/js/bootstrap.bundle.js') }}"></script>
    <script src="{{ asset('assets/js/ChangeReservationHours.js') }}"></script>
    {% block js %}{% endblock %}
    </body>
</html>
