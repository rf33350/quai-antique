{% extends 'base.html.twig' %}



{% block content %}
    <div class="text-center">
        <h1>Administration du menu "{{ menu.title }}"</h1>
    </div>


    <div class="mx-2">
        <a href="{{ path('app_admin_menu_index') }}">Retour à la liste des menus</a>
        <hr>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ menu.id }}</td>
                </tr>
                <tr>
                    <th>Titre</th>
                    <td>{{ menu.title }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_admin_menu_edit', {'id': menu.id}) }}"><button class="btn btn-dark">Modifier</button></a>
        <br>
        <br>
        {{ include('admin_menu/_delete_form.html.twig') }}
    </div>


{% endblock %}
